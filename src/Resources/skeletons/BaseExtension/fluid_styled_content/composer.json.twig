{
    "name": "{{ package.vendorNameAlternative }}/{{ package.packageNameAlternative }}",
    "type": "typo3-cms-extension",
    "description": "{{ package.description}}",
    "homepage": "{{ package.author.homepage }}",
    "license": ["GPL-2.0-or-later"],
    "keywords": ["TYPO3 CMS"],
    "version": "1.0.0",
    "require": {
        "typo3/cms-core": "{% if package.typo3version < 9005000 %}^8.7{% else %}^9.5{% endif %}",
        "typo3/cms-rte-ckeditor": "{% if package.typo3version < 9005000 %}^8.7{% else %}^9.5{% endif %}",
        "typo3/cms-fluid-styled-content": "{% if package.typo3version < 9005000 %}^8.7{% else %}^9.5{% endif %}"
    },
    "autoload": {
        "psr-4": {
            "{{ package.vendorName }}\\{{ package.packageName }}\\": "Classes/"
        }
    }
}
